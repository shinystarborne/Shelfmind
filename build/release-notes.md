## Fixes
- Opening a PDF from Continue Reading now resumes where you stopped instead of restarting at page 1.
- Each Continue Reading card has a ✕ (top-right, on hover) to remove it from the list — handy for things you opened but never actually started. Books go back to Unread, PDFs reopen from page 1 next time.
